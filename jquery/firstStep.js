// Quando o documento estiver pronto executar essa função
        //Coloque na tag body esse texto
        $("document").ready(function(){

            $("body").append("<h1> It's alive </h1>");

        });